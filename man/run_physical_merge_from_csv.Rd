% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/physical_merge.R
\name{run_physical_merge_from_csv}
\alias{run_physical_merge_from_csv}
\title{Read a REML-GPCM CSV and run physical locus merging}
\usage{
run_physical_merge_from_csv(
  path,
  sig_th = 5e-08,
  window = 5e+05,
  reward = "min",
  value_col = "P_HPI",
  chrom = NULL
)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{sig_th}{Significance threshold (length-1 numeric).  A SNP is
significant when \code{value < sig_th} (\code{reward = "min"}) or
\code{value > sig_th} (\code{reward = "max"}).}

\item{window}{Window size in base-pairs (positive numeric).  Controls how
far the algorithm searches ahead for the next signal, and how far blocks
extend on each side of their representative SNP.}

\item{reward}{\code{"min"} (default) for p-values (smaller = more
significant); \code{"max"} for test statistics (larger = more
significant).}

\item{value_col}{Column to use as the merging criterion.  Common choices:
\code{"P_HPI"} (default), \code{"P_Direct"}, \code{"P_Indirect"},
\code{"P_TE"}.}

\item{chrom}{Optional character/integer vector of chromosomes to retain.
\code{NULL} (default) keeps all.}
}
\value{
The merged-block data frame from \code{\link{physical_merge}} with
additional columns \code{CHROM} and \code{rps_ID} (rsID of the
representative SNP, if an \code{ID} column is present in the input).
}
\description{
Convenience wrapper that chains \code{\link{read_gpcm_csv}} and
\code{\link{physical_merge}}, then joins the rsID of each representative
SNP back to the output.
}
\examples{
\dontrun{
blocks <- run_physical_merge_from_csv(
  path      = "stage1_ch1_P_HPI.csv",
  sig_th    = 5e-8,
  window    = 500000,
  value_col = "P_HPI"
)
}
}
