% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/physical_merge.R
\name{read_gpcm_csv}
\alias{read_gpcm_csv}
\title{Read a REML-GPCM per-SNP CSV file}
\usage{
read_gpcm_csv(path, value_col = "P_HPI", chrom = NULL)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{value_col}{Column to use as the merging criterion.  Common choices:
\code{"P_HPI"} (default), \code{"P_Direct"}, \code{"P_Indirect"},
\code{"P_TE"}.}

\item{chrom}{Optional character/integer vector of chromosomes to retain.
\code{NULL} (default) keeps all.}
}
\value{
The input data frame with two extra columns: \code{position} (copy
of \code{POS}, numeric) and \code{value} (copy of \code{value_col},
numeric).  Rows with \code{NA} in either column are dropped.
}
\description{
Reads a Stage 2 REML-GPCM output CSV and prepares it for
\code{\link{physical_merge}}.  The chromosome column \code{"#CHROM"} is
normalised to \code{"CHROM"}, and two interface columns (\code{position},
\code{value}) are appended.
}
\examples{
\dontrun{
df <- read_gpcm_csv("stage1_ch1_P_HPI.csv", value_col = "P_HPI")
}
}
